name: Release

on:
  push:
    branches:
      - main

concurrency: ${{ github.workflow }}-${{ github.ref }}

jobs:
  release:
    name: Release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Dependencies
        run: npm install

      - name: Create Release Pull Request
        uses: changesets/action@v1
        with:
          version: npx changeset version
          # We don't publish automatically since you prefer manual publish
          # but this will create the PR to bump versions and update changelogs
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
